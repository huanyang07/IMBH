# Adaptive hot-exit phase-atlas manifest WP10c9d6c7c3b5c4f25ed

Classification: `adaptive_hot_exit_rank_adaptive_phase_atlas_manifest_frozen`.

The selected event-driven architecture is extended prospectively by at most eight rank-adaptive, eight-node Lobatto windows. Each window uses no more than 15 unique exact continuous-rate states, no nonlinear fixed-Q roots, and no BDF microsteps.

The historical hot-exit event definition is unchanged. A failed phase window is never propagated, and even an observed event authorizes only endpoint refinement and held-out replication—not a complete cycle or reduced slow evolution.
