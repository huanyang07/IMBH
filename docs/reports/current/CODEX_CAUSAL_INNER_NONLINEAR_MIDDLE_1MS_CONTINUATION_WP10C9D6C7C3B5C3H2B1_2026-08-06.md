# Middle 1 ms continuation WP10c9d6c7c3b5c3h2b1

## Classification

`middle_1ms_continuation_passed_2ms_manifest_authorized`

The middle nonlinear base, generic nonlinear anchor, and corrected five-profile tangent reached 1 ms in `9` accepted continuation steps with `0` rejected attempts.

The generic tangent/anchor discrepancies are `1.280658e-09` in state, `8.552355e-09` in instantaneous Tier-I response, and `6.834747e-12` in the cumulative response. Base and anchor last-step serialized replays are bitwise.

The conservative factor-two projection for the remaining middle work to 5 ms is `11.27` hours in the `automatic_continuation` tier.

A pass authorizes only a fresh definitions-only middle 2 ms continuation manifest. Fine work, the 5 ms spatial certificate, fixed-Q experiments, and reduced slow evolution remain blocked.
