# Saved-array branch-candidate screen WP10c9d6c7c3b5c4f25dm

## Result

Classification: `saved_cold_side_candidate_supported_hot_side_not_observed_branch_truth_blocked_bounded_hot_exit_acquisition_manifest_authorized`.

The nearest eligible cold-side state is `full_model_12ms`. Its saved trajectory secant is below the frozen hidden-fraction gate and its complete accepted history and physical margins are present.

No hot-side state passes. The four accepted fixed-Q checkpoints after 20 ms have hidden fractions from `0.999999052` to `0.999999059` and macro distances no larger than `6.872579e-05`. They remain inside the transition sector and may not be relabeled as a hot branch.

The screen used only previously revealed, hash-locked states and algebraic coordinates. It made zero truth-rate calls, generator assemblies, roots, chart retractions, or propagated steps, and it did not open the sealed 16 ms state.

Authorized next artifact: `WP10c9d6c7c3b5c4f25dn`, a definitions-only bounded hot-exit acquisition manifest. Branch truth remains blocked.
