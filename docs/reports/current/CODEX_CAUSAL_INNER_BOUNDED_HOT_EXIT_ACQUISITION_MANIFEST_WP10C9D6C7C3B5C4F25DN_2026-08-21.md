# Bounded hot-exit acquisition manifest WP10c9d6c7c3b5c4f25dn

The saved-array screen found a valid 12 ms cold-side candidate but no hot-side candidate. The accepted fixed-Q warm_3 checkpoint remains transition-internal and is frozen as the only execution seed.

The next package may execute at most 12 equal BDF2 roots of 1.0e-07 s, one root per committed stage. Every root retains the full exact y470 residual and all existing fixed-Q, storage, reaction, ledger, reconstruction, and physical gates.

A candidate exit requires 2 consecutive accepted secants with dual-consistent hidden fraction at most 0.25, rank-16 hidden amplitude at least 0.05, and macro drift from the seed at most 0.02. Hidden rather than macro displacement defines hot-side separation because both fast branches lie on the same slow fiber.

This manifest executes no root and authorizes no branch label, branch root, impulse fit, online solver, or reduced slow evolution.

Authorized next package: `WP10c9d6c7c3b5c4f25do`.
