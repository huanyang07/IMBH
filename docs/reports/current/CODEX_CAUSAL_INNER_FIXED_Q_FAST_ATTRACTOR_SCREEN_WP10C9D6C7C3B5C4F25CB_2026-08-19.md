# Fixed-Q fast-attractor screen WP10c9d6c7c3b5c4f25cb

## Classification

`no_in_chart_stationary_fast_graph_found_departure_amplitude_expansion_required`

The corrected 280D memory block is strictly Hurwitz: spectral abscissa `-3.80703516 s^-1` and condition number `191814.884`.

The rejected 346D block from the naive 96/374 split has `37` nonnegative-real-part eigenvalues, so that split remains blocked.

All `13` bounded searches were boundary-limited. The best reduced residual remained `0.205444364` of the base forcing and no in-chart root passed.

The eliminated memory equilibrium is also far outside the decoded local physical chart for the screened candidates. This is diagnostic evidence that the local chart covers a growing transient segment, not a stationary fast graph.

The only authorized next artifact is `definitions_only_guarded_departure_amplitude_expansion_manifest`. No physical microburst, cycle prediction, or reduced slow evolution is authorized.
