# Shell-gated recentered-atlas manifest WP10c9d6c7c3b5c4f25cg

## Classification

`shell_gated_degree45_recentered_atlas_manifest_frozen`

A minimal degree-4 even / degree-5 odd correction is frozen for both the 560D decoder and 560D state-space rate. It is driven only by the 28D departure coordinate.

The correction is identically zero through load `0.010`, reaches full weight at `0.013`, triggers recentering at `0.012`, and retains `0.015` as a hard limit. Thus all prior inner-chart predictions are bitwise unchanged.

Revealed training maximum decoder/rate errors are `7.177539e-04` / `7.693426e-04`. These are fit diagnostics, not independent validation.

The only authorized next artifact is `WP10c9d6c7c3b5c4f25ch`: geometry-first validation of eight prospectively frozen mixed forward-sector holdouts. No new truth call or trajectory was made.
