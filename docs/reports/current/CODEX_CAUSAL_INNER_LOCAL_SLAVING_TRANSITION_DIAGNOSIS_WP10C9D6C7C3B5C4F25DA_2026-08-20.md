# Local slaving and transition diagnosis WP10c9d6c7c3b5c4f25da

## Classification

`local_slow_graph_slaving_rejected_transition_layer_identified_hybrid_event_map_architecture_authorized`

The conservative 32-to-16-cell Q5 restriction and lifting pass exactly, including the global mass, angular-momentum, and Killing-energy ledgers. The obstruction is therefore not conservative geometry.

The validated three-direction forward tangent has eigenvalues `[-2026691.49184207   -21000.901148       6612.74487418]` per second. One direction is unstable, so treating the whole forward patch as a slaved slow graph is rejected. Promoting only that direction leaves gap `3.175822e+00`, below the frozen gate `10.0`. A two-coordinate transition split leaves a stable fast mode and gap `9.650498e+01`.

The fixed-Q seed cloud has effective macro rank `0` at tolerance `1.0e-08`. It cannot identify branchwise slow closure or stable memory. Those fits were stopped rather than inferred from numerical retraction noise.

The refined architecture is hybrid: multi-second conservative evolution on separately trained cold/hot slow branches, bracketed event surfaces, and a prevalidated offline fast transition map carrying its integrated conservative ledger. The validated forward field seeds that transition map; it is never microstepped around the full cycle online.

Authorized next artifact: `WP10c9d6c7c3b5c4f25db`. It is definitions-only and must freeze separate branch and transition datasets before any new truth. No online solver or cycle is authorized.
