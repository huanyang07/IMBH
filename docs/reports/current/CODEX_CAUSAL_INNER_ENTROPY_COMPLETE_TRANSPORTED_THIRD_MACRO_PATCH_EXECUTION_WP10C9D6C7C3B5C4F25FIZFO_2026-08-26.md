# Entropy-complete transported third macro-patch execution

Classification: `entropy_complete_transported_third_patch_rejected`.

The transported tangent used `9` new calls, a fraction `2.307692e-01` of a full patch. Its maximum blind JVP defect was `4.052101e-01`.

The 12 ms truth output/rate defects were `1.019777e-01` / `1.019777e-01`; the local spectral abscissa was `-5.796477e-01` /s.

No unbounded atlas growth or complete-cycle execution is authorized.

Authorized next: `None`.
