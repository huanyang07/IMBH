# Causal Inner Embedded Endpoint-Regularity Discrimination

## WP10c9d6c7c1b — 2026-07-29

Analyzed base:

```text
d9300e938f6636d92518cc242ac16231a65d6716
```

Frozen manifest:

```text
b230ce7a3c7e7546d0d706ee8f9bcfa3102c6c69be5f67a29aa451e1b5d9706b
```

## Binding classification

```text
no_regularized_embedded_profile_class_selected
```

The strict complete embedded contract is not certified. All 16 frozen C3
endpoint-active and C2 exact-zero-buffer variants pass the direct state and
13-export contract, but inherited characteristic-energy and interface-state
diagnostics fail their unchanged direction gates.

The c7b rejection, c7c0 manifest, c7c1a uniform certification, and every
historical threshold remain unchanged. No alternate route, absolute
reflection threshold, profile change, operator change, or post-result
activity exception is introduced.

This package authorizes no downstream work. In particular, it does not
authorize a bounded nonlinear common mode, production promotion, fixed-Q
experiment, or reduced slow-time evolution.

## Method and ledger gates

All three unchanged embedded monolithic tangents pass:

| Layout | Active export JVP defect | Transport telescope | Active prefix ledger |
|---|---:|---:|---:|
| N128 exterior + N128 inner | `1.783e-8` | `1.070e-16` | `2.076e-16` |
| N128 exterior + N256 inner | `1.811e-8` | `2.807e-16` | `3.048e-16` |
| N128 exterior + N512 inner | `2.858e-8` | `3.274e-16` | `4.234e-16` |

The maximum exact semigroup-integral solve residual is

```text
7.2740e-15
```

and every sign/amplitude propagation-scaling defect is zero.

## Direct state and physical-export result

All 16 variants pass the unchanged direct contract:

```text
direct passes:          16
alternate-route passes:  0
direct failures:          0
```

The direct extrema over all variants are:

| Metric | Instantaneous worst value | Cumulative worst value |
|---|---:|---:|
| RMS order | `2.05386` | `2.04129` |
| Maximum order | `1.96134` | `2.05119` |
| Minimum significant-component order | `1.79519` | `1.75246` |
| Maximum fine normalized difference | `1.1382e-8` | `3.0135e-9` |
| Minimum history cosine | `0.99999989` | `0.99999969` |
| Minimum refinement-error cosine | `0.993203` | `0.990993` |

State convergence also passes:

```text
minimum state order                         1.95917
minimum significant state-component order  1.91414
minimum state error cosine                  0.989759
maximum continuum-reference/fine ratio      0.012880
```

The historical c7b physical-export failure is therefore removed for both
prospective constructions:

- raising the active endpoint from C2 `sin^3` to C3 `sin^4`;
- keeping C2 `sin^3` while ending its support three parent cells before the
  coupling face.

This is strong evidence for an endpoint/stencil regularity crossover in the
direct physical exports. It is not sufficient to pass the complete frozen
contract.

## Coupling-diagnostic result

The coupling-face M/J/E flux itself is below the frozen `1e-8` relative
activity threshold for every profile and passes as inactive:

```text
maximum response/scale:
  buffered C2 profiles  3.78e-10
  active C3 profiles    2.79e-9
```

The complete diagnostic bundle fails because of two characteristic-energy
channels and three interface-state histories:

| Profile/channel | Response/scale or fine difference | RMS order | Error/history cosine | Result |
|---|---:|---:|---:|---|
| buffered C2 inward: incident selected energy | `6.27e-6` response | `2.271` | error `0.8438` | fail |
| active C3 inward: inner-other energy | `1.01e-6` response | `3.751` | error `0.8140` | fail |
| buffered C2 inward: interface stress | `8.96e-11` fine | `6.650` | history/error `0.8728/0.8753` | fail |
| active C3 inward: interface stress | `7.72e-9` fine | `2.909` | history/error `0.8612/0.8707` | fail |
| active C3 outward: interface stress | `7.70e-9` fine | `2.855` | history/error `0.8607/0.8956` | fail |

Every failed diagnostic contracts rapidly and has a very small fine
difference, but the predeclared `0.90` direction gates are binding. The
outward buffered C2 profile passes its complete diagnostic bundle.

No absolute magnitude exception is introduced after observing these values.

## Post-result localization

The nonbinding 13-export sector cosines remain strong:

| Profile | All active exports | Inner + distributed | Coupling + net drive |
|---|---:|---:|---:|
| buffered C2 inward | `0.99761` | `0.99725` | `0.99795` |
| buffered C2 outward | `0.99748` | `0.99710` | `0.99783` |
| active C3 inward | `0.99119` | `0.99222` | `0.99035` |
| active C3 outward | `0.99105` | `0.99195` | `0.99033` |

This supports the direct physical-export pass and shows that the old c7b
coupling-plus-net rotation is absent. The standalone coupling-face error
direction is poorly conditioned because the signal is inactive; it is not
used as an alternate certification route.

## Scientific interpretation

Two statements must be kept separate:

1. **Direct physical result:** both prospective regularity constructions
   remove the c7b physical-export failure and converge near second order.
2. **Complete certification result:** the inherited auxiliary
   characteristic-energy/interface-state direction contract still fails, so
   no regularized embedded class is formally selected.

The result does not select an operator redesign. The coupling-face flux is
inactive and passing, the shared ledgers close, and all failed diagnostic
signals contract faster than second order. Changing the interface operator
would not be evidence-selected.

The most defensible next research package, if this line is continued, is an
operator-neutral, definitions-only diagnostic-observability audit. It must
prospectively distinguish:

- physically meaningful incident/transmitted/reflected energy channels;
- channels smaller than independently measured continuum, projection, or
  roundoff uncertainty;
- state histories whose direction cosine is ill-conditioned because the
  complete signal is nearly zero.

That package may define new held-out manufactured interface-scattering
controls and thresholds before propagation. It may not retroactively pass
c7c1b or reuse the observed magnitudes as fitted cutoffs.

## Recommended bounded next plan

### WP10c9d6c7c2a — definitions-only diagnostic-observability manifest

1. Preserve c7b, c7c0, c7c1a, and c7c1b classifications.
2. Change no physical or numerical operator.
3. Construct independent manufactured interface-scattering profiles with:
   - C3-or-better endpoint regularity;
   - support and propagation direction declared before evaluation;
   - selected incident energy deliberately above continuum/reference
     uncertainty;
   - both shear signs and held-out mixed controls.
4. Freeze fixed physical energy/state scales independent of candidate
   response.
5. Freeze relative activity only from independent uncertainty and slow-impact
   budgets, not from the c7c1b failed values.
6. Require direct physical exports, interface states, selected-family energy,
   leakage, shared-flux ledgers, and exact integrals to pass prospectively.
7. If no diagnostic can be made observable within the certified linear
   regime, classify the auxiliary channel as non-certifying rather than
   tuning its cosine.

Only a later prospective propagation package may determine whether a
physically observable embedded class can be certified.

## Stop gates

Do not:

- amend or relabel c7b-c7c1b;
- lower the `0.90` history/error-cosine gates retroactively;
- add an absolute reflection exception to the current result;
- infer an interface-flux defect from inactive coupling signals;
- redesign the operator;
- begin nonlinear, production, fixed-Q, or reduced slow-time work;
- run N1024 as a rescue.

## Verification

```text
28 passed
```

Canonical evidence is stored in:

```text
results/canonical/
causal_inner_embedded_regularity_wp10c9d6c7c1b/
```
