# Middle 5 ms completion WP10c9d6c7c3b5c3h2d1

## Classification

`middle_5ms_completion_passed_spatial_analysis_and_fine_manifest_authorized`

The middle base, generic nonlinear anchor, and five-profile tangent reached 5 ms in `8` new accepted steps with `0` rejected attempts.

The tangent discrepancy is `8.473546e-07` of the state response and `2.052472e-03` of the instantaneous Tier-I response. Base and anchor last-step replays are bitwise.

A pass authorizes only spatial analysis and a fresh fine campaign manifest. Fixed-Q experiments and reduced slow evolution remain blocked.
