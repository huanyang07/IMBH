# First nonlinear duration-rung manifest WP10c9d6c7c3b5c1a

## Classification

`first_nonlinear_duration_rung_manifest_frozen_two_e_minus_four_second_propagation_authorized`

This definitions-only package freezes the first geometric duration rung. It changes no operator or production default and propagates no state.

## Frozen experiment

- coarse background plus `p3_buffer45__generic_five_field` response
- horizon `2.0e-04 s`; outputs every `2e-5 s`
- restart/replay from `1.0e-04 s`
- strict shadow over `1.6e-04-2.0e-04 s` with maximum step `1e-5 s`
- main local/summed error budgets: `2.5e-4` / `5e-3`
- correct active coupling face: `48`

## Scope

The horizon is only a controller-depth/restart rung: about `0.0361` of one N128 cell-crossing time. A pass authorizes only the definitions-only `1e-3 s` second-rung manifest. Fixed-Q and reduced evolution remain blocked.
