# Adaptive original-free-field complete-cycle execution

Classification: `complete_cycle_inconclusive_acquisition_budget_exhausted`.

The execution acquired `64` patches with `192` exact original-free-field witnesses in `13.903` wall hours.

Cycle observed: `False`; persistent mode switches: `18`; cycle duration: `None` s.

Maximum blind rate defect: `5.015829e-03`; maximum embedded correction: `5.189798e-03`; restart bitwise: `False`.

All fixed-Q physical-rate/reaction calls, nonlinear roots, and BDF microsteps remained zero. Reduced slow evolution remains separately gated.
