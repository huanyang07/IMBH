# Entropy-complete hyperbolicity-boundary refinement diagnostic

Classification: `entropy_complete_refined_candidate_hyperbolicity_failure_persists`.

The saved 0.5 ms failure reproduced: `True`. Both 0.25 and 0.125 ms full-truth probes passed: `False`.

The scan bracketed the boundary between `0.0001875` and `0.00021875` s from the accepted 179 ms endpoint.

Every probe was nonpropagating. The parent rejection remains binding and complete-cycle execution remains unauthorized.

Authorized next: `None`.
