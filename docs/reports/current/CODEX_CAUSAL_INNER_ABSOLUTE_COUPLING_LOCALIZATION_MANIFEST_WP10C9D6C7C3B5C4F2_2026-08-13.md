# Absolute coupling-localization manifest

Classification: `absolute_coupling_localization_manifest_frozen_existing_state_audit_authorized`.

This definitions-only package freezes a four-time, six-face decomposition using committed states. It introduces no physical lift, no trajectory, and no relaxation of the failed absolute baseline gate.

The next analysis distinguishes native-state and shared-parent operator contributions at and immediately inside the embedded transition. Memory and fixed-Q propagation remain blocked.
