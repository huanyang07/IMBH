# Entropy-complete conservative macro-atlas execution

Classification: `entropy_complete_conservative_16_cell_macro_atlas_rejected`.

Offline truth calls: `0`; maximum independent colored-JVP defect: `nan`.

Held-out base passed: `False`; held-out perturbed passed: `False`.

The `0`-evaluation online benchmark took `0.000000` s with zero online truth calls, roots, or propagation.

Failure: `{'stage': 'first_colored_raw_M_coordinate_plus_lift', 'type': 'LinAlgError', 'message': 'Singular matrix', 'interpretation': 'raw_independent_MJE_atlas_coordinate_left_the_locally_invertible_thermodynamic_cone'}`.

Authorized next: `None`.
