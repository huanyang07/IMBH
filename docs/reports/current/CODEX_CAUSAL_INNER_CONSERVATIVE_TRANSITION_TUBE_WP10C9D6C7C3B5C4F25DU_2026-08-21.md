# Conservative transition tube WP10c9d6c7c3b5c4f25du

## Classification

`train_only_rank_adaptive_conservative_scalar_transition_tube_validated_local_observed_segment`

The train-only model selected hidden rank `2` with one dynamic progress coordinate. Maximum held-out full error/path is `1.584441e-03`; maximum fine-secant relative error is `1.630963e-02`.

The online lift requires approximately `78960` flops and `322712` stored bytes. The 82-coordinate conservative decoder and partial ledger telescope close within `1.658319e-20`.

Failed gates: `[]`.

The observed terminal state is not a hot exit. The partial macro reset must not be used as a complete transition impulse.
