# Entropy-complete selective transport-refresh manifest

Classification: `entropy_complete_lnSigma_betaPhi_selective_refresh_manifest_frozen`.

The unchanged transported tangent rejection is preserved. Saved full-patch evidence selects only `lnSigma` and `beta_phi` for a fresh colored refresh; the other three input-field columns remain exact chain-rule transports.

The selected two-field hybrid has maximum 128-direction saved-Jacobian probe defect `2.542384e-02`. The prospective package uses 21 truth calls versus 39 for a full rebuild.

No unbounded atlas growth or complete-cycle execution is authorized.

Authorized next: `WP10c9d6c7c3b5c4f25fizfq_entropy_complete_selectively_refreshed_third_patch_execution` only.
