# Causal Inner Prospective Band-Envelope Validation

## WP10c9d6c6f1 — 2026-07-29

Analyzed base:

```text
7005aa11bb22f40862abf886bc3f7fee26ec68b8
```

Frozen manifest:

```text
221a271dd861226bbc09eaf430dfc6bef47ad39a5b5d7e6e53520f9d75fcb643
```

## Binding classification

```text
prospective_band_envelope_uniform_validation_certified
```

The unchanged uniform monolithic operator is certified for the declared
resolved profile class. Embedded export discrimination is authorized as the
next definitions-first package.

This result does not authorize a nonlinear physical trajectory, production
promotion, a fixed-Q micro-solver, or reduced slow-time evolution.

The historical c6c rejection remains unchanged. Its failed `sin^2` shear
profiles are outside this prospective validation set and cannot be passed
retroactively.

## Frozen validation set

The five previously frozen and eligible base profiles were:

```text
p3__inward_shear
p3__outward_shear
p5__inward_shear
p5__outward_shear
p3__material
```

Both signs and amplitude factors `0.5` and `1.0` produced 20 binding
variants. Projection replay found all 45 expected N128/N256/N512 arrays,
with no missing or mismatched hash.

The exact frozen manifest was not changed after propagation.

## Main result

All 20 variants pass the original direct state and 13-export contract.
No profile required the cancellation-conditioned alternate route.

```text
direct variants:       20
alternate variants:     0
failed variants:        0
```

The extrema over every sign/amplitude variant and both instantaneous and
cumulative histories are:

| Quantity | Worst measured value | Gate |
|---|---:|---:|
| RMS order | `1.9119517` | `>=0.75` |
| Maximum order | `1.8473587` | `>=0.75` |
| Significant-component order | `1.1743183` | `>=0.75` |
| Fine normalized difference | `2.0904e-7` | `<=0.05` |
| History cosine | `0.99999785` | `>=0.90` |
| Refinement-error cosine | `0.98369689` | `>=0.90` |

The direct result is substantially inside every frozen threshold.

## Method and replay checks

The N128/N256/N512 method gates all pass, with zero incoming excision
characteristics. The maximum exact semigroup-integral relative solve
residual is

```text
7.9869e-15
```

and the maximum sign/amplitude propagation-scaling defect is zero.

The largest continuum-reference uncertainty divided by the fine spatial
difference is

```text
3.8362e-8
```

which is far below the frozen `0.10` gate.

## Band-envelope diagnostic

The alternate band-envelope route was computed as an audit but was not used
to pass any variant. Across the five base profiles:

| Diagnostic | Maximum |
|---|---:|
| Cancellation ratio | `0.148077` |
| Absolute band-error envelope | `3.85315e-8` |
| Direct band-sum defect | `1.77236e-15` |
| Signed Gram closure defect | `5.01702e-14` |
| Continuum uncertainty / fine difference | `3.83616e-8` |

The route therefore remains mathematically consistent and prospectively
frozen, but this package supplies no empirical necessity test for it:
ordinary direct component convergence was sufficient in all 20 cases.

## Interpretation

This package closes the uniform resolved-profile question for the declared
profile class:

- ordinary smooth `p3` and `p5` shear profiles converge;
- the held-out `p3` material profile converges;
- sign and linear amplitude scaling are exact;
- both instantaneous and cumulative physical exports converge;
- the prior lower-height-work cancellation pathology does not recur.

The certification is intentionally limited. It does not cover spectrally
ineligible narrow packets, arbitrary perturbations, embedded coupling, or a
nonlinear finite-amplitude residual.

## Authorized next step

Freeze a separate WP10c9d6c7 embedded-discrimination manifest before any new
propagation. That package must determine:

1. the exact fixed N128 exterior and N128/N256/N512-equivalent inner layouts;
2. common physical extraction faces and reconstruction halos;
3. which frozen analytic profiles remain fully inside the refined domain;
4. exact embedded projections and hashes;
5. coupling-interface reflection and transmission diagnostics;
6. unchanged physical-export gates and fail-fast decisions.

Only profiles satisfying the same N128 resolution and purity contract may be
binding. If the existing full-domain profiles overlap the coupling stencil,
the manifest must stop rather than silently taper, shift, or redefine them.

## Stop gates

Do not:

- alter or relabel the c6c rejection;
- change the operator, profile definitions, or thresholds;
- use the alternate envelope route outside lower-height-work angular momentum;
- begin embedded propagation before a definitions-only layout/profile audit;
- begin nonlinear, production, fixed-Q, or reduced slow-time work;
- run N1024 as a rescue.

## Canonical evidence

```text
results/canonical/
causal_inner_band_envelope_validation_wp10c9d6c6f1/
```

The numerical propagation completed in approximately `1282.75 s`.
