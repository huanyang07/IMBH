# Causal Inner Endpoint-Regularity Uniform Controls

## WP10c9d6c7c1a — 2026-07-29

Analyzed base:

```text
406ab7e18d6094d07647efb91dccce06808087b4
```

Frozen manifest:

```text
b230ce7a3c7e7546d0d706ee8f9bcfa3102c6c69be5f67a29aa451e1b5d9706b
```

## Binding classification

```text
endpoint_interface_regularity_uniform_controls_certified_
embedded_discrimination_authorized
```

All 16 frozen sign/amplitude variants of the four WP10c9d6c7c0
endpoint/interface controls pass the unchanged uniform N128/N256/N512 state
and 13-export contract by the direct route. No alternate envelope,
conditioning exception, operator change, profile change, or threshold change
is used.

This authorizes only WP10c9d6c7c1b embedded regularity discrimination.
The negative WP10c9d6c7b result remains binding for its original complete
profile class. Nonlinear physical evolution, production promotion, fixed-Q
experiments, and reduced slow-time evolution remain blocked.

## Frozen controls

The exact c7c0 controls are preserved:

| Profile | Endpoint class | Coupling trace | Variants | Uniform result |
|---|---|---|---:|---|
| `p4__inward_shear` | C3 at coupling face 48 | active | 4 | pass |
| `p4__outward_shear` | C3 at coupling face 48 | active | 4 | pass |
| `p3_buffer45__inward_shear` | C2 at face 45 | exact-zero buffer | 4 | pass |
| `p3_buffer45__outward_shear` | C2 at face 45 | exact-zero buffer | 4 | pass |

Each profile is propagated with signs `+/-` and amplitude factors `0.5/1.0`.
Linearity gives zero sign/amplitude propagation-scaling defect.

## Method and exact-integral gates

The unchanged monolithic uniform tangents pass on N64, N128, N256, and N512.
N64 remains a construction/reference control; the binding propagation triplet
is N128/N256/N512.

The maximum exact semigroup-integral relative solve residual is

```text
7.3464e-15
```

against the unchanged `1e-12` gate.

## State convergence

Across all 16 variants:

```text
minimum state observed order                 1.96556
minimum significant state-component order   1.93562
minimum state refinement-error cosine        0.989331
maximum N128/Richardson state error           7.8256e-4
maximum continuum-reference/fine ratio        0.010826
maximum projection/fine ratio                 1.9143e-11
maximum restart/fine ratio                    3.3349e-11
maximum boundary-integral/fine ratio          1.2468e-9
```

Every state/reference gate passes.

## Physical-export convergence

The direct 13-export results over all variants are:

| Metric | Instantaneous worst value | Cumulative worst value |
|---|---:|---:|
| RMS order | `1.99550` | `1.92875` |
| Maximum order | `1.95938` | `1.93943` |
| Minimum significant-component order | `1.95290` | `1.86351` |
| Maximum fine normalized difference | `1.7537e-8` | `1.2116e-9` |
| Minimum history cosine | `0.99999959` | `0.99999807` |
| Minimum refinement-error cosine | `0.996169` | `0.996011` |

All are well inside the frozen gates:

```text
RMS order                         >= 0.75
maximum order                     >= 0.75
significant-component order       >= 0.75
fine normalized difference        <= 0.05
history cosine                    >= 0.90
refinement-error cosine           >= 0.90
reference uncertainty/fine error  <= 0.10
```

The two prospective classes therefore have clean uniform continuum behavior.
Any difference found in c7c1b can be attributed to the embedded layout and
coupling interaction rather than a failure already present on uniform grids.

## Scientific interpretation

This result does not decide between the c7c0 endpoint/interface hypotheses.
It establishes the required clean control:

- C3 endpoint-active `p4` shear profiles converge uniformly;
- C2 exact-zero-buffer `p3` shear profiles converge uniformly;
- sign and amplitude do not affect the classification;
- the strict instantaneous refinement-error-direction gate is stable for all
  four base profiles.

The embedded comparison must now preserve the same definitions and evaluate
the frozen decision table:

| Embedded result | Interpretation |
|---|---|
| C3 `p4` and buffered C2 `p3` both pass | certify the declared smoother/buffered embedded class; no operator redesign |
| C3 `p4` fails, buffered C2 `p3` passes | active coupling-endpoint hypothesis selected; local truncation audit only |
| C3 `p4` passes, buffered C2 `p3` fails | short-support/global `p3` crossover; no interface redesign |
| both classes fail | no regularized embedded class selected |

Association remains insufficient to prove an interface defect. An operator
change may be considered only if a resolved, uniformly certified control
exhibits a stable local coupling truncation mechanism.

## Authorized next package

WP10c9d6c7c1b may:

1. propagate all 16 unchanged variants on the three frozen c7a embedded
   layouts;
2. use the same active-domain 13 exports, common faces, shared coupling flux,
   prefix ledgers, interface states, and characteristic-energy diagnostics;
3. require all original state, instantaneous, cumulative, method, exact
   integral, reconstruction, and ledger gates;
4. classify the four base profiles using the frozen c7c0 decision table.

It may not change the coupling radius, profile support, endpoint power,
zero-buffer width, activity threshold, physical scales, or error-cosine gate.

## Stop gates

Do not:

- amend c7b or c7c0;
- use a band-envelope alternate route;
- drop a failed sign/amplitude sibling;
- tune the coupling operator from the historical `p3` association;
- begin a nonlinear trajectory, production promotion, fixed-Q experiment, or
  reduced slow-time evolution;
- run N1024 as a rescue.

## Verification

```text
20 passed
```

Canonical evidence is stored in:

```text
results/canonical/
causal_inner_embedded_regularity_uniform_wp10c9d6c7c1a/
```
