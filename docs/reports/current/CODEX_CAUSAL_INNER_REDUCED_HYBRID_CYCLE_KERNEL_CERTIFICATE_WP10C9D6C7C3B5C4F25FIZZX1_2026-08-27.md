# Reduced hybrid cycle-kernel certificate

Classification: `reduced_hybrid_cycle_kernel_structure_and_cost_certified_synthetic_fixture_only`.

The adapter advanced `22` accepted synthetic prefix steps through `2` finite events, re-audited every endpoint, closed the reduced ledger, and replayed a restarted suffix bitwise. Production metadata rejects this fixture. The measured online projection is `3.974279e-03` wall days for 100,000 macrosteps.

This is a code-path and cost certificate, not physical calibration. The physical bundle and independent heldouts remain absent; no complete-cycle step occurred.
